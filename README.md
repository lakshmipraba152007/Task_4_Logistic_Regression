# Task_4_Logistic_Regression
This project builds a binary classification model using Logistic Regression on the Breast Cancer Wisconsin Dataset. It includes data preprocessing, feature scaling, train-test splitting, model training, prediction, and evaluation using accuracy, precision, recall, confusion matrix, ROC-AUC, threshold tuning, and sigmoid function visualization.
